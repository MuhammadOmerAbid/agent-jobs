def cover(job_id: str) -> None:
    raise NotImplementedError(
        "Cover note generation is deferred to a fast-follow pass — see project plan section E. "
        f"(requested for job_id={job_id})"
    )
